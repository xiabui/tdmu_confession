import 'package:flutter/material.dart';

class MakeAPost extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Post"),),
    );
  }
}